AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Deploys the Well-Architected Foundations Assessment for AWS Organizations,
  including automated AWS CodeBuild execution, scoped IAM permissions, encrypted
  and versioned Amazon S3 report storage, and optional Amazon SNS email
  notifications.

Parameters:
  EmailAddress:
    Type: String
    Default: ''
    Description: >
      (Optional) Email address for assessment completion notifications.
      Leave blank to skip SNS topic creation.

  SourceBucket:
    Type: String
    Description: S3 bucket name containing the assessment source archive.

  SourceKey:
    Type: String
    Default: 'wa-foundations-source.zip'
    Description: S3 object key for the assessment source archive.

Conditions:
  CreateSNSTopic: !Not [!Equals [!Ref EmailAddress, '']]

Resources:
  # ============================================================================
  # S3 Bucket — Report storage (FR-1.2, NFR-1.3)
  # ============================================================================
  WAFAResultsBucket:
    Type: AWS::S3::Bucket
    # Explicit naming avoids repetitive stack/logical-ID combinations. Retain
    # the previous bucket if an earlier temporary stack is updated.
    DeletionPolicy: Delete
    UpdateReplacePolicy: Retain
    Properties:
      BucketName:
        Fn::Join:
          - ''
          - - 'wa-foundations-results-'
            - Fn::Select:
                - 2
                - Fn::Split:
                    - '/'
                    - !Ref 'AWS::StackId'
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      VersioningConfiguration:
        Status: Enabled
      LoggingConfiguration:
        LogFilePrefix: wa-foundations-access-logs/
      Tags:
        - Key: Application
          Value: WA-Foundations-Assessment

  # ============================================================================
  # IAM Role — ReadOnly assessment permissions (FR-1.2, NFR-1.1, NFR-1.2)
  # ============================================================================
  WAFARole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${AWS::StackName}-codebuild-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: codebuild.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - !Ref WAFAAssessmentPolicy

  WAFAAssessmentPolicy:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: Read-only assessment and report upload permissions
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          # Assessment read-only permissions
          - Effect: Allow
            Action:
              - sts:GetCallerIdentity
              - organizations:DescribeOrganization
              - organizations:ListAWSServiceAccessForOrganization
              - organizations:ListRoots
              - organizations:ListAccounts
              - organizations:ListOrganizationalUnitsForParent
              - organizations:ListDelegatedAdministrators
              - organizations:ListDelegatedServicesForAccount
              - controltower:ListLandingZones
              - controltower:GetLandingZone
              - cur:DescribeReportDefinitions
              - bcm-data-exports:ListExports
              - cloudtrail:DescribeTrails
              - config:DescribeConfigurationRecorders
              - config:DescribeConfigurationRecorderStatus
              - config:DescribeDeliveryChannels
              - config:DescribeDeliveryChannelStatus
              - ec2:DescribeInstances
              - ec2:DescribeVpcs
              - ec2:DescribeRegions
              - iam:ListUsers
              - sso:ListInstances
              - cloudformation:DescribeOrganizationsAccess
            Resource: '*'
          # S3 write for report upload
          - Effect: Allow
            Action:
              - s3:PutObject
            Resource: !Sub '${WAFAResultsBucket.Arn}/*'
          # S3 read for source bucket
          - Effect: Allow
            Action:
              - s3:GetBucketAcl
              - s3:GetBucketLocation
              - s3:GetBucketVersioning
            Resource: !Sub 'arn:${AWS::Partition}:s3:::${SourceBucket}'
          - Effect: Allow
            Action:
              - s3:GetObject
              - s3:GetObjectVersion
              - s3:GetObjectTagging
              - s3:GetObjectVersionTagging
            Resource: !Sub 'arn:${AWS::Partition}:s3:::${SourceBucket}/*'
          # CloudWatch Logs for CodeBuild (scoped to specific log group)
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
            Resource:
              - !Sub 'arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/codebuild/${AWS::StackName}'
          - Effect: Allow
            Action:
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource:
              - !Sub 'arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/codebuild/${AWS::StackName}:*'
          # SNS publish (conditional)
          - !If
            - CreateSNSTopic
            - Effect: Allow
              Action:
                - sns:Publish
              Resource: !Ref WAFANotificationTopic
            - !Ref 'AWS::NoValue'

  # ============================================================================
  # CodeBuild Project — Assessment execution (FR-1.2)
  # ============================================================================
  WAFAProject:
    Type: AWS::CodeBuild::Project
    Properties:
      Name: !Ref 'AWS::StackName'
      Description: Well-Architected Foundations Assessment — automated governance check
      Source:
        Type: S3
        Location: !Sub '${SourceBucket}/${SourceKey}'
        BuildSpec: buildspec.yml
      Environment:
        Type: LINUX_CONTAINER
        ComputeType: BUILD_GENERAL1_SMALL
        Image: aws/codebuild/standard:7.0
        EnvironmentVariables:
          - Name: RESULTS_BUCKET
            Value: !Ref WAFAResultsBucket
          - Name: SNS_TOPIC_ARN
            Value: !If [CreateSNSTopic, !Ref WAFANotificationTopic, '']
      ServiceRole: !GetAtt WAFARole.Arn
      Artifacts:
        Type: NO_ARTIFACTS
      TimeoutInMinutes: 10
      Tags:
        - Key: Application
          Value: WA-Foundations-Assessment

  # ============================================================================
  # SNS Topic — Optional notification (FR-1.2, FR-10.5)
  # ============================================================================
  WAFANotificationTopic:
    Type: AWS::SNS::Topic
    Condition: CreateSNSTopic
    Properties:
      TopicName: !Sub '${AWS::StackName}-notifications'
      KmsMasterKeyId: alias/aws/sns

  WAFANotificationSubscription:
    Type: AWS::SNS::Subscription
    Condition: CreateSNSTopic
    Properties:
      TopicArn: !Ref WAFANotificationTopic
      Protocol: email
      Endpoint: !Ref EmailAddress

  # ============================================================================
  # Custom Resource Lambda — Auto-trigger CodeBuild on stack creation (FR-1.3)
  # ============================================================================
  WAFATriggerLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - !Ref WAFATriggerPolicy

  WAFATriggerPolicy:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: Permissions for the assessment trigger Lambda
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Action:
              - codebuild:StartBuild
            Resource: !GetAtt WAFAProject.Arn
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
            Resource:
              - !Sub 'arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/lambda/WAFATriggerLambda-*'
          - Effect: Allow
            Action:
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource:
              - !Sub 'arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/lambda/WAFATriggerLambda-*:*'

  WAFATriggerLambda:
    Type: AWS::Lambda::Function
    Properties:
      Runtime: python3.12
      Handler: index.handler
      Role: !GetAtt WAFATriggerLambdaRole.Arn
      Timeout: 30
      Code:
        ZipFile: |
          import boto3
          import cfnresponse

          def handler(event, context):
              if event['RequestType'] == 'Delete':
                  cfnresponse.send(event, context, cfnresponse.SUCCESS, {})
                  return

              try:
                  project_name = event['ResourceProperties']['ProjectName']
                  client = boto3.client('codebuild')
                  client.start_build(projectName=project_name)
                  cfnresponse.send(event, context, cfnresponse.SUCCESS, {
                      'Message': f'Build started for {project_name}'
                  })
              except Exception as e:
                  cfnresponse.send(event, context, cfnresponse.FAILED, {
                      'Error': str(e)
                  })

  WAFATrigger:
    Type: Custom::WAFATrigger
    Properties:
      ServiceToken: !GetAtt WAFATriggerLambda.Arn
      ProjectName: !Ref WAFAProject

Outputs:
  ResultsBucket:
    Description: Name of the S3 bucket containing generated assessment reports
    Value: !Ref WAFAResultsBucket

  ResultsURL:
    Description: AWS Management Console URL for the assessment results bucket
    Value: !Sub 'https://s3.console.aws.amazon.com/s3/buckets/${WAFAResultsBucket}'

  CodeBuildProject:
    Description: Name of the CodeBuild project used to run or re-run the assessment
    Value: !Ref WAFAProject

  CodeBuildConsoleURL:
    Description: AWS Management Console URL for the CodeBuild project build history
    Value: !Sub 'https://${AWS::Region}.console.aws.amazon.com/codesuite/codebuild/projects/${WAFAProject}/history'
